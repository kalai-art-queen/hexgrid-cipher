# hexgrid-cipher
Reference implementation for "Graph Labelling Techniques and Their
Cryptographic Applications."

## Requirements
python >= 3.10, numpy, scipy, sympy

    pip install numpy scipy sympy

## Reproduce the experiments
    python hexgrid_eval.py

All experiments use a fixed seed, so the printed numbers match the
tables in Section "Experimental Evaluation" of the paper.
